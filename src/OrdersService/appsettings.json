{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information",
      "OrdersService.Controllers": "Trace"
    }
  },
  "OrdersService": {
    "CreateOrderPubSubName": "orders",
    "CreateOrderTopicName": "new_orders",
    "IdentityServer": {
      "Authority": "<SET_THIS>",
      "RequireHttpsMetadata": true
    },
    "ConnectionString": "" // Set this value to target a MS SQL database, otherwise you'll use the in-memory EF Core provider
  },
  "AllowedHosts": "*"
}
